node_modules/
.next/
# La compilación del sandbox local (`npm run prod:local`).
.next-local/
out/
build/
.env
.env.local
*.tsbuildinfo
.firebase/
functions/lib/
functions/node_modules/
serviceAccount*.json
.DS_Store
*-debug.log
firebase-export-*/

.vercel
.env*
!.env.example

# Runtimes de Python y R copiados desde node_modules (scripts/copy-code-runtimes.mjs).
public/runtime/

# Sandbox local (scripts/dev-local.mjs). Datos de DynamoDB Local, borrables.
.local-sandbox/

# El JAR de ECJ vive en public/runtime/java/, ya ignorado arriba: lo descarga
# `scripts/fetch-java-toolchain.mjs` verificando su SHA-256.
# Igual el JAR del spike J0 y las fuentes que genera cada corrida.
spikes/j0-cheerpj/assets/

# Playwright (tests/e2e). Informes y trazas de una ejecución local.
/test-results/
/playwright-report/
/blob-report/
/.playwright/
